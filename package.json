{
  "name": "ci-check",
  "version": "1.0.0",
  "description": "Checks the status of the latest successful run of a specified workflow across multiple repositories.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/musialpatryk/ci-check.git"
  },
  "license": "ISC",
  "author": "musialpatryk",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "scripts": {
    "build": "ncc build index.js --minify --license licenses.txt -o dist"
  }
}
